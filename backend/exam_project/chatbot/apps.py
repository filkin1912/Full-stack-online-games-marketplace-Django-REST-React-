from django.apps import AppConfig


class ChatbotConfig(AppConfig):
    """Django app configuration for the chatbot module."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "exam_project.chatbot"
